# Section 2: forward-QP strong-convexity floor sweep.
# How sensitive is diff_full recovery to the eps regularizer?
experiment: epsilon_sweep
run_name: epsilon_sweep
seeds: [0, 1, 2]
network: {n_buses: 8, n_lines: 12}
data: {n_train: 150, n_val: 80, obs_noise: 0.5}
model: {n_strata: 1}
training: {steps: 300, lr: 0.05, early_stopping_patience: 80}
forward: {physics: transport}
extra:
  eps_values: [0.01, 0.05, 0.1, 0.3, 1.0]
